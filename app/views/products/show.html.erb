<h1><%=@product.name%></h1>
<%=render partial: 'listings/listings', locals: {listings: @product.listings, index: false}%><br>

<%=link_to "Add New Listing", new_product_listing_path(@product) %><br><br>
<%=link_to "Edit",  edit_product_path(@product)%><br><br>

<%=button_to("Delete", @item, method: :delete, data: { confirm: "Really?" }) %>